{
  "name": "zcloudpass-extension",
  "private": true,
  "version": "1.1.0",
  "description": "CI helpers for the zCloudPass browser extension",
  "scripts": {
    "test": "node scripts/validate-extension.mjs",
    "validate": "node scripts/validate-extension.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
